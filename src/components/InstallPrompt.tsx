export function InstallPrompt() {
  // Club Nanny should not show an automatic install popup on page load.
  // The intentional install entry points are the footer button and the
  // post-payment submission screens.
  return null;
}
